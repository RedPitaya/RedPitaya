#!/bin/sh

case "$1" in
        start)
                echo -n "Starting nginx ... "
                LD_LIBRARY_PATH=/opt/redpitaya/lib /opt/redpitaya/sbin/scpi-server
                echo "done."
                ;;
        stop)
                echo -n "Stopping nginx ..."
                killall scpi-server
                echo "done."
                ;;
        restart)
                $0 stop
                sleep 1
                $0 start
                ;;
        *)
                echo "usage: $0 {start|stop|restart}"
                ;;
esac
